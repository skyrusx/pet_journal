<% set_seo_meta(
     title: "Дневник питомца — здоровье и напоминания | PetJournal",
     description: "Ведите дневник питомца в PetJournal: здоровье, прививки, документы и напоминания в одном месте. PetTag с QR-профилем поможет, если питомец потеряется.",
     og_title: "PetJournal — всё важное о питомце рядом",
     og_description: "Журнал здоровья, напоминания, документы и PetTag — чтобы важная информация о питомце всегда была под рукой."
   ) %>

<div class="pj-new-design">
  <%= render "new_design_navbar" %>

  <main class="pj-new-design__canvas" aria-label="PetJournal">
    <%= render "new_design_hero" %>
    <%= render "new_design_showcase" %>
    <%= render "new_design_steps" %>
    <%= render "new_design_features" %>
    <%= render "new_design_trust_strip" %>
    <%= render "new_design_cta" %>
  </main>

  <%= render "new_design_footer" %>
</div>
