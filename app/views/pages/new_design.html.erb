<% set_seo_meta(
     title: "PetJournal — цифровой дневник питомца",
     description: "PetJournal помогает хранить историю жизни и здоровья питомца в одном месте, не забывать о важном и быстро находить нужную информацию, когда она нужна.",
     og_title: "PetJournal — всё важное о питомце рядом",
     og_description: "Личное пространство для истории вашего питомца — чтобы важное не терялось, а заботиться о нём было проще каждый день."
   ) %>

<div class="pj-new-design">
  <%= render "new_design_navbar" %>

  <main class="pj-new-design__canvas" aria-label="PetJournal">
    <%= render "new_design_hero" %>
    <%= render "new_design_showcase" %>
    <%= render "new_design_steps" %>
    <%= render "new_design_features" %>
    <%= render "new_design_trust_strip" %>
    <%= render "new_design_cta" %>
  </main>

  <%= render "new_design_footer" %>
</div>
