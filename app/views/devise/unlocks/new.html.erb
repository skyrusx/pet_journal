<div class="auth-page">
  <section class="auth-card">
    <div class="auth-copy">
      <%= link_to root_path, class: "auth-brand", aria: { label: "PetJournal — главная" } do %>
        <%= image_tag "petjournal/logo-horizontal.png", alt: "PetJournal", width: 172, height: 49 %>
      <% end %>
      <span>Разблокировка</span>
      <h1>Восстановим вход в аккаунт</h1>
      <p>Укажите эл. почту, чтобы получить письмо для разблокировки аккаунта.</p>
      <ul>
        <li>Инструкция придёт на почту аккаунта</li>
        <li>Это помогает защитить данные питомцев</li>
        <li>После разблокировки можно войти снова</li>
      </ul>
      <div class="auth-pettag-art" aria-hidden="true">
        <span class="auth-pettag-ring"></span>
        <span class="auth-pettag-qr"></span>
        <span class="auth-pettag-label">PetTag</span>
      </div>
    </div>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
      <div class="auth-form-heading">
        <span>Аккаунт</span>
        <h2>Получить инструкцию</h2>
      </div>

      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="mb-3">
        <%= f.label :email, "Эл. почта", class: "form-label" %>
        <%= f.email_field :email, autocomplete: "email", class: "form-control" %>
      </div>

      <%= f.submit "Отправить инструкцию", class: "btn btn-primary w-100 auth-submit" %>
    <% end %>

    <%= render "devise/shared/links" %>
  </section>
</div>
