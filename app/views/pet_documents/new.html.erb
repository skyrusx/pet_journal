<div class="py-4">
  <div class="page-heading mb-4">
    <div>
      <div class="text-muted small mb-1"><%= @pet.name %></div>
      <h1 class="h3 mb-1">Новый документ</h1>
      <p class="text-muted mb-0">Загрузите файл, задайте тип и срок действия.</p>
    </div>
  </div>

  <%= render "form", pet: @pet, document: @document %>
</div>
