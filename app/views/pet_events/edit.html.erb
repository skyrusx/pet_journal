<div class="form-page py-4">
  <div class="form-page-header">
    <div>
      <div class="text-muted small mb-1"><%= @pet.name %></div>
      <h1 class="h3 mb-1">Редактировать событие</h1>
      <p class="text-muted mb-0">Обновите дату, описание или вложения записи.</p>
    </div>
    <%= link_to "Открыть событие", pet_pet_event_path(@pet, @pet_event), class: "btn btn-outline-secondary" %>
  </div>

  <%= render "form" %>
</div>
